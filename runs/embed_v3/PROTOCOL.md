EMBED under the corrected weight update (2026-09-21). The regret arms use the SIGNED excess L_g - R_g (no clamp at zero), gamma chosen on validation max-excess from {0.5, 2.0, 8.0}: regret_only 0.5, ours 2.0 (jobs g0.5 / g2.0 / g8.0 hold all three). erm, anchors_only, groupdro and align_only are copied unchanged from runs/final_embed: they have no reference term (R* = 0), so the clamp was never active for them, and EMBED already had diagonal anchors, validation-only selection and train+val references.

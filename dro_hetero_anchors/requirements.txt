torch>=2.2
torchvision>=0.17
numpy>=1.24
pandas>=1.5
scikit-learn>=1.2
pyyaml>=6.0
rich>=13.4
tqdm>=4.66
tensorboard>=2.16
matplotlib>=3.7
datasets>=2.14

# Fed-Heart Disease: optional FLamby (pip install git+https://github.com/owkin/FLamby.git)
# If not installed, we auto-download UCI data; no flamby needed.

# EMBED mammography (DICOM reading). Needs AWS access — see documentation/EMBED_PLAN.md
# pydicom + JPEG plugins are required to decode compressed mammography DICOMs.
pydicom>=2.4
pylibjpeg>=2.0
pylibjpeg-libjpeg>=2.0
python-gdcm>=3.0
awscli>=1.32   # download from s3://embed-dataset-open (us-west-2)